apiVersion: v1
kind: Service
metadata:
  name: {{ include "ds-demo.fullname" . }}
  labels:
    {{- include "ds-demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ds-demo.selectorLabels" . | nindent 4 }}
